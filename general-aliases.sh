#General Aliases

alias myip="curl http://ipecho.net/plain" #display ip address
alias hisg="history | grep" #searches you command history
alias ps-grep="ps aux | grep" # e.g."ps-grep java" will list processes that have java in description
alias ping="ping -o" #pings and stops
alias rm="rm -i" #moves to trash
alias power-off='sudo /sbin/shutdown -h now' # This forces a shutdown.

alias shrug="echo '¯\_(ツ)_/¯' | pbcopy";
alias fight="echo '(ง'̀-'́)ง' | pbcopy";
